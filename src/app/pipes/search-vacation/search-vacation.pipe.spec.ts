import { SearchVacationPipe } from './search-vacation.pipe';

describe('SearchVacationPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchVacationPipe();
    expect(pipe).toBeTruthy();
  });
});
