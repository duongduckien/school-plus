import { PupilFilterGroupPipe } from './pupil-filter-group.pipe';

describe('PupilFilterGroupPipe', () => {
	it('create an instance', () => {
		const pipe = new PupilFilterGroupPipe();
		expect(pipe).toBeTruthy();
	});
});
