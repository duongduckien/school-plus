import { PupilFilterAgentPipe } from './pupil-filter-agent.pipe';

describe('PupilFilterAgentPipe', () => {
  it('create an instance', () => {
    const pipe = new PupilFilterAgentPipe();
    expect(pipe).toBeTruthy();
  });
});
