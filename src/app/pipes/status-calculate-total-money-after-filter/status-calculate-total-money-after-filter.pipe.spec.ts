import { StatusCalculateTotalMoneyAfterFilterPipe } from './status-calculate-total-money-after-filter.pipe';

describe('StatusCalculateTotalMoneyAfterFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new StatusCalculateTotalMoneyAfterFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
